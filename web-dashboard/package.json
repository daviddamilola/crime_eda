{
  "name": "cartel-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:cartel-data": "node scripts/generate-cartel-data.mjs",
    "build": "npm run generate:cartel-data && npx --yes -p typescript tsc",
    "watch": "tsc --watch",
    "serve": "npx --yes serve -l 5173 ."
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
